<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HIMASISFOR - Tentang Kami</title>
    <link rel="stylesheet" href="assets/css/tentang.css">
</head>

<body>
    <!-- Floating Particles -->
    <div class="particles" id="particles"></div>

    <nav class="navbar">
        <div class="nav-content">
            <div class="logo">HIMASISFOR</div>
            <ul class="nav-links">
                <li><a href="index.php">Beranda</a></li>
                <li><a href="tentang.php" class="active">Tentang</a></li>
                <li><a href="visimisi.php">Visi & Misi</a></li>
                <li><a href="struktur.php">Struktur</a></li>
                <li><a href="kegiatan.php">Kegiatan</a></li>
                <li><a href="berita.php">Berita</a></li>
                <li><a href="artikel.php">Artikel</a></li>
            </ul>
            <div class="hamburger" id="hamburger">
                <span></span>
                <span></span>
                <span></span>
            </div>

            <div class="mobile-menu" id="mobileMenu">
                <ul>
                    <li><a href="index.php">Beranda</a></li>
                    <li><a href="tentang.php" class="active">Tentang</a></li>
                    <li><a href="visimisi.php">Visi & Misi</a></li>
                    <li><a href="struktur.php">Struktur</a></li>
                    <li><a href="kegiatan.php">Kegiatan</a></li>
                    <li><a href="berita.php">Berita</a></li>
                    <li><a href="artikel.php">Artikel</a></li>
                </ul>
            </div>

            <div class="overlay" id="overlay"></div>

        </div>
    </nav>

    <!-- Mobile Menu -->
    <div class="mobile-menu" id="mobileMenu">
        <ul>
            <li><a href="index.php" onclick="closeMobileMenu()">Beranda</a></li>
            <li><a href="tentang.php" class="active" onclick="closeMobileMenu()">Tentang</a></li>
            <li><a href="visimisi.php" onclick="closeMobileMenu()">Visi & Misi</a></li>
            <li><a href="struktur.php" onclick="closeMobileMenu()">Struktur</a></li>
            <li><a href="kegiatan.php" onclick="closeMobileMenu()">Kegiatan</a></li>
            <li><a href="berita.php" onclick="closeMobileMenu()">Berita</a></li>
            <li><a href="artikel.php" onclick="closeMobileMenu()">Artikel</a></li>
        </ul>
    </div>

    <!-- Hero Section -->
    <section class="hero">
        <div class="hero-content">
            <h1>Tentang HIMASISFOR</h1>
            <p>Mengenal lebih dalam organisasi mahasiswa yang berdedikasi untuk kemajuan teknologi informasi</p>
        </div>
    </section>

    <!-- Main Content -->
    <main class="main-content">
        <div class="container">
            <!-- Introduction Section -->
            <div class="full-width-content">
                <h3>Profil Organisasi</h3>
                <p>HIMASISFOR (Himpunan Mahasiswa Sistem Informasi) merupakan organisasi mahasiswa tingkat program studi yang bernaung di bawah Program Studi Sistem Informasi, Universitas Teknologi Akba Makassar (UNITAMA). Organisasi ini didirikan dengan tujuan utama menjadi wadah aspirasi, kreativitas, dan pengembangan potensi mahasiswa dalam bidang sistem informasi dan teknologi.</p>
                <p>Sebagai organisasi yang dinamis dan progresif, HIMASISFOR berkomitmen untuk terus berinovasi dalam mengembangkan program-program yang bermanfaat bagi kemajuan mahasiswa, program studi, dan universitas.</p>
            </div>

            <!-- Statistics -->
            <div class="content-section">
                <h2 class="section-title">Fakta & Statistik</h2>
                <div class="stats-grid">
                    <div class="stat-card">
                        <div class="stat-number">150+</div>
                        <div class="stat-label">Anggota Aktif</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-number">25+</div>
                        <div class="stat-label">Program Kerja</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-number">50+</div>
                        <div class="stat-label">Kegiatan per Tahun</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-number">10+</div>
                        <div class="stat-label">Kerjasama</div>
                    </div>
                </div>
            </div>

            <!-- About Details -->
            <div class="content-section">
                <h2 class="section-title">Profil Lengkap</h2>
                <div class="content-grid">
                    <div class="content-card">
                        <h3>Sejarah Berdiri</h3>
                        <p>HIMASISFOR didirikan pada tahun 2020 sebagai respons terhadap kebutuhan akan wadah organisasi yang spesifik untuk mahasiswa Sistem Informasi di UNITAMA. Organisasi ini lahir dari semangat para mahasiswa untuk menciptakan komunitas yang solid dan produktif.</p>
                        <p>Sejak berdirinya, HIMASISFOR telah mengalami perkembangan yang signifikan dalam hal keanggotaan, program kerja, dan kontribusi terhadap kemajuan program studi.</p>
                    </div>

                    <div class="content-card">
                        <h3>Tujuan Organisasi</h3>
                        <p>Adapun tujuan utama HIMASISFOR adalah:</p>
                        <ul>
                            <li>Menjadi wadah aspirasi dan kreativitas mahasiswa Sistem Informasi</li>
                            <li>Mengembangkan potensi akademik dan non-akademik mahasiswa</li>
                            <li>Memfasilitasi kegiatan yang mendukung pembelajaran teknologi informasi</li>
                            <li>Menjalin kerjasama dengan berbagai pihak untuk pengembangan organisasi</li>
                            <li>Mencetak lulusan yang kompeten dan berkarakter</li>
                        </ul>
                    </div>

                    <div class="content-card">
                        <h3>Nilai-Nilai Organisasi</h3>
                        <p>HIMASISFOR menjunjung tinggi nilai-nilai berikut dalam setiap aktivitasnya:</p>
                        <ul>
                            <li><strong>Integritas:</strong> Menjaga kejujuran dan transparansi dalam setiap tindakan</li>
                            <li><strong>Inovasi:</strong> Mendorong kreativitas dan pemikiran out-of-the-box</li>
                            <li><strong>Kolaborasi:</strong> Membangun kerjasama yang solid antar anggota</li>
                            <li><strong>Excelence:</strong> Selalu berusaha memberikan yang terbaik</li>
                            <li><strong>Empowerment:</strong> Memberdayakan setiap anggota untuk berkembang</li>
                        </ul>
                    </div>

                    <div class="content-card">
                        <h3>Komitmen Kami</h3>
                        <p>HIMASISFOR berkomitmen untuk:</p>
                        <ul>
                            <li>Mengembangkan ekosistem pembelajaran yang kondusif</li>
                            <li>Memfasilitasi pengembangan soft skills dan hard skills</li>
                            <li>Menjembatani hubungan mahasiswa dengan industri</li>
                            <li>Mengoptimalkan pemanfaatan teknologi dalam kegiatan organisasi</li>
                            <li>Berkontribusi positif bagi masyarakat melalui berbagai program</li>
                        </ul>
                    </div>
                </div>
            </div>

            <!-- Timeline Section -->
            <div class="content-section">
                <h2 class="section-title">Perjalanan Organisasi</h2>
                <div class="timeline">
                    <div class="timeline-item">
                        <div class="timeline-content">
                            <span class="timeline-date">2020</span>
                            <h4>Pendirian HIMASISFOR</h4>
                            <p>Pendirian resmi HIMASISFOR sebagai organisasi mahasiswa Program Studi Sistem Informasi UNITAMA dengan 25 anggota pendiri.</p>
                        </div>
                    </div>
                    <div class="timeline-item">
                        <div class="timeline-content">
                            <span class="timeline-date">2021</span>
                            <h4>Pengembangan Program</h4>
                            <p>Peluncuran program-program unggulan seperti workshop teknologi, seminar IT, dan kegiatan pengabdian masyarakat pertama.</p>
                        </div>
                    </div>
                    <div class="timeline-item">
                        <div class="timeline-content">
                            <span class="timeline-date">2022</span>
                            <h4>Ekspansi Kerjasama</h4>
                            <p>Menjalin kerjasama dengan berbagai perusahaan teknologi dan organisasi mahasiswa lainnya untuk memperluas jaringan.</p>
                        </div>
                    </div>
                    <div class="timeline-item">
                        <div class="timeline-content">
                            <span class="timeline-date">2023</span>
                            <h4>Digitalisasi Organisasi</h4>
                            <p>Implementasi sistem manajemen organisasi digital dan peluncuran platform online untuk kegiatan mahasiswa.</p>
                        </div>
                    </div>
                    <div class="timeline-item">
                        <div class="timeline-content">
                            <span class="timeline-date">2024</span>
                            <h4>Pencapaian Prestasi</h4>
                            <p>Meraih berbagai penghargaan tingkat regional dan nasional dalam kompetisi teknologi informasi.</p>
                        </div>
                    </div>
                    <div class="timeline-item">
                        <div class="timeline-content">
                            <span class="timeline-date">2025</span>
                            <h4>Era Baru Kepemimpinan</h4>
                            <p>Dimulainya periode kepengurusan 2025-2026 dengan visi baru untuk mengembangkan organisasi ke level yang lebih tinggi.</p>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Future Plans -->
            <div class="full-width-content">
                <h3>Rencana Masa Depan</h3>
                <p>HIMASISFOR memiliki visi jangka panjang untuk menjadi organisasi mahasiswa terdepan dalam bidang sistem informasi di Indonesia. Kami berencana untuk mengembangkan berbagai program inovatif, memperluas jaringan kerjasama internasional, dan terus berkontribusi dalam pengembangan teknologi informasi di Indonesia.</p>
                <p>Dengan dukungan dari seluruh anggota, alumni, dan stakeholder, kami yakin dapat mencapai visi tersebut dan memberikan dampak positif yang lebih besar bagi dunia pendidikan dan teknologi.</p>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <p>&copy; 2025 HIMASISFOR UNITAMA. All rights reserved.</p>
            <p>Himpunan Mahasiswa Sistem Informasi - Universitas Teknologi Akba Makassar</p>
            <div class="footer-links">
                <a href="index.html">Beranda</a>
                <a href="about.html">Tentang</a>
                <a href="vision.html">Visi & Misi</a>
                <a href="structure.html">Kepemimpinan</a>
                <a href="activities.html">Kegiatan</a>
            </div>
        </div>
    </footer>

    <script>
        // Particle System
        function createParticles() {
            const particlesContainer = document.getElementById('particles');
            const particleCount = 30;

            for (let i = 0; i < particleCount; i++) {
                const particle = document.createElement('div');
                particle.className = 'particle';
                particle.style.left = Math.random() * 100 + '%';
                particle.style.top = Math.random() * 100 + '%';
                particle.style.width = Math.random() * 4 + 2 + 'px';
                particle.style.height = particle.style.width;
                particle.style.animationDelay = Math.random() * 6 + 's';
                particle.style.animationDuration = (Math.random() * 3 + 3) + 's';
                particlesContainer.appendChild(particle);
            }
        }

        // Navbar scroll effect
        window.addEventListener('scroll', function() {
            const navbar = document.querySelector('.navbar');
            if (window.scrollY > 50) {
                navbar.style.background = 'rgba(0, 0, 0, 0.98)';
            } else {
                navbar.style.background = 'rgba(0, 0, 0, 0.95)';
            }
        });

        // Intersection Observer for animations
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -100px 0px'
        };

        const observer = new IntersectionObserver(function(entries) {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.opacity = '1';
                    entry.target.style.transform = 'translateY(0)';
                }
            });
        }, observerOptions);

        // Counter Animation
        function animateCounters() {
            const counters = document.querySelectorAll('.stat-number');

            counters.forEach(counter => {
                const target = parseInt(counter.textContent.replace('+', ''));
                const increment = target / 50;
                let current = 0;

                const updateCounter = () => {
                    if (current < target) {
                        current += increment;
                        counter.textContent = Math.floor(current) + '+';
                        requestAnimationFrame(updateCounter);
                    } else {
                        counter.textContent = target + '+';
                    }
                };

                updateCounter();
            });
        }

        // Initialize
        document.addEventListener('DOMContentLoaded', function() {
            createParticles();

            // Observe all cards for animation
            const cards = document.querySelectorAll('.content-card, .stat-card, .timeline-item, .full-width-content');
            cards.forEach(card => {
                card.style.opacity = '0';
                card.style.transform = 'translateY(30px)';
                card.style.transition = 'all 0.6s ease-out';
                observer.observe(card);
            });

            // Animate counters when stats section is visible
            const statsSection = document.querySelector('.stats-grid');
            if (statsSection) {
                const statsObserver = new IntersectionObserver(function(entries) {
                    entries.forEach(entry => {
                        if (entry.isIntersecting) {
                            animateCounters();
                            statsObserver.unobserve(entry.target);
                        }
                    });
                }, {
                    threshold: 0.5
                });

                statsObserver.observe(statsSection);
            }
        });

        // Mobile menu toggle
        const mobileMenu = document.querySelector('.mobile-menu');
        const navLinks = document.querySelector('.nav-links');

        if (mobileMenu) {
            mobileMenu.addEventListener('click', function() {
                navLinks.style.display = navLinks.style.display === 'flex' ? 'none' : 'flex';
                navLinks.style.position = 'absolute';
                navLinks.style.top = '100%';
                navLinks.style.left = '0';
                navLinks.style.width = '100%';
                navLinks.style.background = 'rgba(0, 0, 0, 0.95)';
                navLinks.style.flexDirection = 'column';
                navLinks.style.padding = '20px';
                navLinks.style.gap = '20px';
            });
        }

        // Smooth scrolling for internal links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function(e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });



        // Particle System
        function createParticles() {
            const particlesContainer = document.getElementById('particles');
            const particleCount = 30;

            for (let i = 0; i < particleCount; i++) {
                const particle = document.createElement('div');
                particle.className = 'particle';
                particle.style.left = Math.random() * 100 + '%';
                particle.style.top = Math.random() * 100 + '%';
                particle.style.width = Math.random() * 4 + 2 + 'px';
                particle.style.height = particle.style.width;
                particle.style.animationDelay = Math.random() * 6 + 's';
                particle.style.animationDuration = (Math.random() * 3 + 3) + 's';
                particlesContainer.appendChild(particle);
            }
        }

        // Filter functionality
        function initializeFilters() {
            const filterTabs = document.querySelectorAll('.filter-tab');
            const categories = document.querySelectorAll('.category-section');

            filterTabs.forEach(tab => {
                tab.addEventListener('click', () => {
                    // Remove active class from all tabs
                    filterTabs.forEach(t => t.classList.remove('active'));
                    // Add active class to clicked tab
                    tab.classList.add('active');

                    const selectedCategory = tab.getAttribute('data-category');

                    // Show/hide categories based on filter
                    categories.forEach(category => {
                        if (selectedCategory === 'all' || category.classList.contains(selectedCategory)) {
                            category.style.display = 'block';
                            category.style.opacity = '0';
                            setTimeout(() => {
                                category.style.opacity = '1';
                            }, 100);
                        } else {
                            category.style.opacity = '0';
                            setTimeout(() => {
                                category.style.display = 'none';
                            }, 300);
                        }
                    });
                });
            });
        }

        // Counter Animation
        function animateCounters() {
            const counters = document.querySelectorAll('.stat-number');

            counters.forEach(counter => {
                const target = parseInt(counter.getAttribute('data-target'));
                const increment = target / 50;
                let current = 0;

                const updateCounter = () => {
                    if (current < target) {
                        current += increment;
                        counter.textContent = Math.floor(current);
                        requestAnimationFrame(updateCounter);
                    } else {
                        counter.textContent = target;
                    }
                };

                updateCounter();
            });
        }

        // Navbar scroll effect
        window.addEventListener('scroll', function() {
            const navbar = document.querySelector('.navbar');
            if (window.scrollY > 50) {
                navbar.style.background = 'rgba(0, 0, 0, 0.98)';
            } else {
                navbar.style.background = 'rgba(0, 0, 0, 0.95)';
            }
        });

        // Staggered animation for activity cards
        function animateActivityCards() {
            const activitySections = document.querySelectorAll('.category-section');

            activitySections.forEach(section => {
                const cards = section.querySelectorAll('.activity-card');
                const sectionObserver = new IntersectionObserver(function(entries) {
                    entries.forEach(entry => {
                        if (entry.isIntersecting) {
                            cards.forEach((card, index) => {
                                setTimeout(() => {
                                    card.style.opacity = '1';
                                    card.style.transform = 'translateY(0)';
                                }, index * 150);
                            });
                            sectionObserver.unobserve(entry.target);
                        }
                    });
                }, {
                    threshold: 0.2
                });

                sectionObserver.observe(section);
            });
        }

        // Initialize
        document.addEventListener('DOMContentLoaded', function() {
            createParticles();
            initializeFilters();

            // Set initial states for animations
            const activityCards = document.querySelectorAll('.activity-card');
            activityCards.forEach(card => {
                card.style.opacity = '0';
                card.style.transform = 'translateY(30px)';
                card.style.transition = 'all 0.6s ease-out';
            });

            // Initialize staggered animations
            animateActivityCards();

            // Observe other elements for animation
            const animatedElements = document.querySelectorAll('.featured-activity, .stats-section');
            animatedElements.forEach(element => {
                element.style.opacity = '0';
                element.style.transform = 'translateY(30px)';
                element.style.transition = 'all 0.8s ease-out';
                observer.observe(element);
            });

            // Stats animation observer
            const statsObserver = new IntersectionObserver(function(entries) {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        setTimeout(animateCounters, 500);
                        statsObserver.unobserve(entry.target);
                    }
                });
            }, {
                threshold: 0.3
            });

            const statsSection = document.querySelector('.stats-section');
            if (statsSection) {
                statsObserver.observe(statsSection);
            }
        });

        const hamburger = document.querySelector('.hamburger');
        const overlay = document.querySelector('.overlay');

        if (hamburger && mobileMenu && overlay) {
            hamburger.addEventListener('click', () => {
                hamburger.classList.toggle('active');
                mobileMenu.classList.toggle('active');
                overlay.classList.toggle('active');
            });

            overlay.addEventListener('click', () => {
                hamburger.classList.remove('active');
                mobileMenu.classList.remove('active');
                overlay.classList.remove('active');
            });
        }

        // Add click handlers for activity cards
        document.querySelectorAll('.activity-card').forEach(card => {
            card.addEventListener('click', function() {
                // Add ripple effect
                const ripple = document.createElement('div');
                ripple.style.position = 'absolute';
                ripple.style.borderRadius = '50%';
                ripple.style.background = 'rgba(255, 255, 255, 0.3)';
                ripple.style.transform = 'scale(0)';
                ripple.style.animation = 'ripple 0.6s linear';
                ripple.style.left = '50%';
                ripple.style.top = '50%';
                ripple.style.width = '20px';
                ripple.style.height = '20px';
                ripple.style.marginLeft = '-10px';
                ripple.style.marginTop = '-10px';

                this.style.position = 'relative';
                this.appendChild(ripple);

                setTimeout(() => {
                    ripple.remove();
                }, 600);
            });
        });

        // Add ripple keyframes
        const style = document.createElement('style');
        style.textContent = `
            @keyframes ripple {
                to {
                    transform: scale(4);
                    opacity: 0;
                }
            }
        `;
        document.head.appendChild(style);
        
    </script>
</body>

</html>